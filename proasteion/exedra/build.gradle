description = 'Local UI adapter – generic Swing shell framework.'

dependencies {
    testImplementation 'junit:junit:4.13.2'
}

tasks.withType(Test).configureEach {
    testLogging {
        events "skipped"
    }
}
