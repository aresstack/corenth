package com.aresstack.corenth.proasteion.platform.network;

/**
 * Ordered stage type within a network route plan.
 */
public enum NetworkRouteStageType {
    DIRECT,
    PLATFORM_PROXY,
    SECURE_GATEWAY
}
